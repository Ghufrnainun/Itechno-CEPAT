info:
  name: "200"
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/auth/login
  body:
    type: json
    data: |-
      {
           "email": "testuser1@example.com",
           "password": "PasswordKuat123!"
         }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

scripts:
  post: |-
    const data = res.getBody();
    if (data.success && data.data && data.data.session) {
      bru.setEnvVar("access_token", data.data.session.access_token);
    }
